@model CurrentXpose_Admin.Entidades.Condominio

@{
    ViewData["Title"] = "Excluir Condomínio";
}

<link rel="stylesheet" href="~/css/exclusao.css" asp-append-version="true" />

<h1 class="agenda-title">Excluir Condomínio</h1>

<div class="container">
    <p>Você está prestes a excluir o condomínio <strong>@Model.nome</strong>. Tem certeza que deseja continuar?</p>

    <form asp-action="Deletar" asp-controller="Condominio" method="post">
        <input type="hidden" asp-for="id" />

        <button type="submit" class="btn btn-danger btn-equal">Confirmar Exclusão</button>
    </form>

    <div class="d-flex justify-content-start mt-2">
        <a class="btn btn-secondary btn-sm" asp-action="Lista" asp-controller="Condominio">Voltar</a>
    </div>
</div>